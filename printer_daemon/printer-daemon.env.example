# Printer Daemon Environment Configuration
# Copy this file to /etc/sysconfig/printer-daemon and customize the values

# MQTT Broker Configuration
MQTT_BROKER_HOST=mqtt.example.com
MQTT_BROKER_PORT=8883
MQTT_USERNAME=printer_user
MQTT_PASSWORD=printer_password
MQTT_CLIENT_ID=printer_daemon_client
MQTT_USE_TLS=true

# MQTT Topics Configuration (subscribes to scale data topic for printing)
MQTT_PRINT_TOPIC=laboratory/scale/data

# MQTT Connection Settings
MQTT_QOS=2
MQTT_KEEPALIVE=60

# Python Runtime Configuration
PYTHONUNBUFFERED=1

# Development/Testing (set to false or remove for production)
# MOCK_SERIAL_DEVICES=false
